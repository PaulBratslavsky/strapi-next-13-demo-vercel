/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  images: {
    domains: ['rigid-spicy-smash.media.strapiapp.com'],
  },
}

module.exports = nextConfig
